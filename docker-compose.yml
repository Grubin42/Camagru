version: "3.8"

networks:
  camagru:
    driver: bridge

volumes:
  shared-seeds: {} # Define a shared volume for seeding 

services:
  mailpit:
    image: axllent/mailpit:latest
    container_name: mailpit
    ports:
      - "1025:1025"
      - "8025:8025"
    networks:
      - camagru

  frankenphp:
    build:
      context: ./frankenphp
      dockerfile: Dockerfile
    container_name: frankenphp
    ports:
      - "80:80"
      - "443:443"
      - "443:443/udp"
    volumes:
      - ./app:/var/www/html/app
      - ./frankenphp/Caddyfile:/etc/caddy/Caddyfile
      - ./frankenphp/php.ini:/usr/local/etc/php/php.ini
      - ./postgresql/hashPassword.php:/docker-entrypoint-initdb.d/hashPassword.php # Mount here for PHP
      - shared-seeds:/docker-entrypoint-initdb.d/shared-seeds
    depends_on:
      - postgresql
      - mailpit
    networks:
      - camagru
    environment:
      POSTGRES_HOST: ${POSTGRES_HOST}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}

  postgresql:
    #why? garder mes données
    image: postgres:16
    restart: unless-stopped
    container_name: postgres
    ports:
      - "5432:5432"
    volumes:
      - ./postgresql/data:/var/lib/postgresql/data
      - ./postgresql/init.sql:/docker-entrypoint-initdb.d/init.sql
      - ./postgresql/seed_posts.sql:/docker-entrypoint-initdb.d/seed_posts.sql
      - shared-seeds:/docker-entrypoint-initdb.d/shared-seeds
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
      TZ: "Europe/Zurich"
    networks:
      - camagru
